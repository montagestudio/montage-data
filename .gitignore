# Paths within each section are in alphabetical order.

# Operating system generated files that shouldn't be committed.
._*
.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes

# Developer-specific IDE files that shouldn't be committed.
*.xcodeproj
.idea
atlassian-ide-plugin.xml

# Build and test generated files that shouldn't be committed.
/builds
/out
TEST-*.xml

# Run-time generated files that shouldn't be committed.
node_modules
npm-debug.log
